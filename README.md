# MindNote - 本地优先的灵感笔记应用

一个跨平台笔记应用，支持 PC 和 Android，随时记录灵感，智能检索，接入 LLM 实现灵感激发。

## ✨ 特性

- 📝 **快速记录**：3秒内创建并保存笔记
- 🔍 **智能搜索**：支持全文搜索、标签搜索、语义搜索
- 🤖 **LLM 集成**：接入大语言模型，实现灵感激发和内容补充
- 🏷️ **标签管理**：智能标签推荐、标签统计
- 📤 **导出功能**：支持导出为 Markdown 文件
- 📱 **跨平台**：支持 PC (Windows/Linux/macOS) 和 Android
- 💾 **本地优先**：所有数据本地存储，保护隐私
- ⭐ **收藏功能**：快速收藏和筛选重要笔记

## 🚀 快速开始

### 前置条件

- Flutter SDK 3.0+
- Dart SDK 3.0+
- VS Code 或 Android Studio

### 安装依赖

```bash
flutter pub get
```

### 运行应用

```bash
flutter run
```

### 构建 APK

```bash
flutter build apk --release
```

## 📁 项目结构

```
mindnote/
├── lib/
│   ├── main.dart              # 应用入口
│   ├── models/                # 数据模型
│   │   └── note.dart          # 笔记模型
│   ├── screens/               # 页面
│   │   ├── home_screen.dart           # 首页
│   │   ├── note_editor_screen.dart    # 笔记编辑
│   │   └── search_screen.dart         # 搜索页面
│   ├── services/              # 服务
│   │   ├── database_service.dart      # SQLite 数据库
│   │   ├── llm_service.dart           # LLM API 集成
│   │   ├── search_service.dart        # 搜索服务
│   │   ├── embedding_service.dart     # 向量嵌入（语义搜索）
│   │   └── export_service.dart        # 导出服务
│   └── widgets/               # 通用组件
│       └── common_widgets.dart        # 高亮文本、标签芯片等
├── tests/                     # 测试
├── pubspec.yaml               # 依赖配置
└── README.md
```

## 🛠️ 技术栈

- **框架**：Flutter
- **本地存储**：SQLite + drift
- **语义搜索**：向量嵌入（可扩展为真实 embedding API）
- **LLM 集成**：DeepSeek API / OpenAI API
- **状态管理**：Provider

## 📋 功能迭代

### v0.2.0 (当前版本)
- ✅ 语义搜索（向量嵌入）
- ✅ 搜索高亮显示
- ✅ 标签管理
- ✅ Markdown 导出
- ✅ 收藏功能

### v0.1.0
- ✅ 笔记 CRUD
- ✅ 本地 SQLite 存储
- ✅ 简单搜索（LIKE 查询）
- ✅ DeepSeek API 集成
- ✅ 灵感激发功能

### v1.0.0 (计划中)
- [ ] 云同步（Firebase / Supabase）
- [ ] 多端数据同步（CRDT）
- [ ] 手写/语音输入
- [ ] 图片附件
- [ ] 真实向量库集成（Pinecone / Qdrant）

## 🔧 配置 LLM API

在 `lib/main.dart` 中配置你的 API Key：

```dart
LLMService(
  config: LLMConfig.deepseek('YOUR_DEEPSEEK_API_KEY'),
),
```

或使用 OpenAI：

```dart
LLMService(
  config: LLMConfig.openai('YOUR_OPENAI_API_KEY'),
),
```

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

## 📄 许可证

MIT License

---

*Built with ❤️ by OpenClaw Agent System*
